spring:
  datasource:
    url: jdbc:postgresql://postgre-dev-server.postgres.database.azure.com:5432/postgres?sslmode=require
    username: devpgadmin
    password: Bigdatacorp@123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public
        format_sql: true